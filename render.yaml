services:
  # Next.js Frontend
  - type: web
    name: vulcan-web
    runtime: node
    region: ohio
    plan: free
    rootDir: apps/web
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_ORCHESTRATOR_URL
        fromService:
          type: web
          name: vulcan-orchestrator
          property: host

  # Python Orchestrator / API
  - type: web
    name: vulcan-orchestrator
    runtime: python
    region: ohio
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn agents.core.api:app -w 2 -k uvicorn.workers.UvicornWorker
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: DESKTOP_SERVER_URL
        sync: false
      - key: REDIS_URL
        fromService:
          type: redis
          name: vulcan-redis
          property: connectionString

  # Redis Cache (Cost Optimization)
  - type: redis
    name: vulcan-redis
    region: ohio
    plan: free
    maxmemoryPolicy: allkeys-lru
