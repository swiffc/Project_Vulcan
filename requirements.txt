# Project Vulcan - Cloud Orchestrator Dependencies
# For Render deployment
# Install: pip install -r requirements.txt

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0

# HTTP Client (for desktop server proxy)
httpx==0.26.0
aiohttp==3.9.1

# LLM
anthropic==0.18.1

# Vector Memory & RAG
chromadb==0.4.22
sentence-transformers==2.2.2

# Cache (Cost Optimization)
redis==5.0.1

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# Validation
pydantic==2.5.3

# System Manager (scheduler + metrics)
APScheduler==3.10.4
psutil==5.9.7

# PDF Processing (for CAD Agent)
pdf2image==1.16.3
pytesseract==0.3.10
Pillow==10.2.0

# Google Drive Integration
google-api-python-client==2.111.0
google-auth==2.25.2

# Browser Automation (Phase 10 - Work Hub)
playwright==1.40.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
numpy<2.0.0  # Fix NumPy 2.0 incompatibility

# Additional dependencies for validators (Phase 19.1.3)
ezdxf>=1.1.0  # DXF file analysis
reportlab>=4.0.0  # PDF generation in validators
PyPDF2>=3.0.0  # PDF annotation feature
fluids>=1.0.0  # Pipe fitting calculations (used in standards scripts)

# Logging (Phase 19.4.21)
python-json-logger>=2.0.0  # JSON logging formatter
